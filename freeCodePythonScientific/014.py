# A função print() fornece apenas uma saída no console, mas funções e métodos podem ter um valor de retorno que você pode usar em seu código.
# Agora atribua alpha.find(text[0]) a uma variável chamada index. Desta forma, o índice armazenará o valor retornado por alfabeto.find(text[0]).

text = 'Hello World'
shift = 3
alphabet = 'abcdefghijklmnopqrstuvwxyz'
index = alphabet.find(text[0])